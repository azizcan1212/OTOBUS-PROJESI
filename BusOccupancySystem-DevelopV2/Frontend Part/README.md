
## Bus Information Frontend

React + Vite tabanli bu uygulama dashboard ve `/{id}` detay ekranlarini Spring Boot backend uzerinden besler.

### Environment

`VITE_API_BASE_URL=http://localhost:8080`

### Komutlar

`npm install`

`npm run dev`

`npm run build`
  
